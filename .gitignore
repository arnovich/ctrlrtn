# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
mutants/

# Local data store (recorded traffic — never commit). The -shm/-wal siblings
# appear while a WAL database is open.
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
data/

# Local campaign artifacts (labels, verdict JSONs) — the published report
# lives in docs/, generated via `ctrlrtn campaign-report`.
campaign/

# Env
.env
